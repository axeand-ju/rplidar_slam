[project]
name = "rplidar-slam-demo"
version = "0.1.0"
description = "Live SLAM demo for the RPLidar C1 with browser dashboard"
requires-python = ">=3.10"
dependencies = [
    "rplidar-roboticia",
    "websockets>=12.0",
    "numpy",
    "scipy",
]

[project.scripts]
slam-demo = "slam_server:main"
